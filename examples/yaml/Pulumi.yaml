name: provider-kind-native
runtime: python
plugins:
  providers:
    - name: kind
      path: /root/go/bin # path to the provider binary for local development and testing

resources:
  myArbitraryKindClusterResourceName:
    type: kind:Kind
    properties:
      name: test