name: provider-kind-native
runtime: yaml
plugins:
  providers:
    - name: kind
      path: /home/vscode/go/bin # relative path to the provider binary for local development and testing

resources:
  myArbitraryKindClusterResourceName:
    type: kind:Kind
    properties:
      name: test
