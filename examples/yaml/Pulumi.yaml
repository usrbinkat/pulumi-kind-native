name: consume-kind-native
runtime: yaml

plugins:
  providers:
    - name: kind
      path: /root/go/bin

resources:
  myArbitraryKindClusterResourceName:
    type: kind:Kind
    properties:
      name: test
